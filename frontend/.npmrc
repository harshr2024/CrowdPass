# npm 10 can mis-resolve Vitest's optional browser peers without legacy peer traversal.
# Every direct dependency remains exact-pinned and the lockfile is still mandatory in CI.
legacy-peer-deps=true
