# Copy to .env to override local defaults. .env is gitignored.
# If you change these, set DB_URL / DB_USERNAME / DB_PASSWORD for the app to match.
POSTGRES_DB=crowdpass
POSTGRES_USER=crowdpass
POSTGRES_PASSWORD=crowdpass
POSTGRES_PORT=5432
REDIS_PORT=6379
ELASTICMQ_PORT=9324
